{
  "name": "automatic-fan-tuner",
  "version": "0.1.0",
  "private": true,
  "description": "Early automatic fan curve optimizer with synthetic load tests, calibration flow, JSON fan profiles, and hardware adapter boundaries.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node test/optimizer.test.mjs && node test/load-app.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/optimizer.js && node --check public/sim-adapter.js && node --check public/cpu-worker.js"
  }
}
